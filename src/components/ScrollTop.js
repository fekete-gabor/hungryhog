import styled from "styled-components";

const ScrollTop = () => {
  return (
    <Wrapper>
      <h2>ScrollTop</h2>
    </Wrapper>
  );
};

const Wrapper = styled.section``;

export default ScrollTop;
