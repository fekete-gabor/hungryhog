import styled from "styled-components";

const GalleryPage = () => {
  return (
    <Wrapper>
      <h2>GalleryPage</h2>;
    </Wrapper>
  );
};

const Wrapper = styled.section`
  width: 100%;
  height: fit-content;
`;

export default GalleryPage;
